# 🌳 Fenwick Tree
Визуализатор дерева Фенвика — это Java приложение для визуализации работы дерева Фенвика. Проект поддерживает светлую и тёмную тему.
## Особенности
### Основной функционал
Построение дерева из исходного массива
Случайная генерация массивов с настраиваемыми параметрами
#### Все основные операции:
- ```update(index, delta)``` - точечное обновление
- ```prefixSum(index)``` - префиксная сумма
- ```rangeSum(left, right)``` - сумма на отрезке
- ```lowerBound(target)``` - нижняя граница по сумме
### Визуализация
__Два режима отображения:__
- __Дерево узлов__ - классическое представление дерева
- __Отрезки__ - визуализация диапазонов ответственности

__Интерактивная навигация:__
- Масштабирование колесиком мыши
- Перетаскивание
- Навигация по большим массивам

### Интерфейс
- Светлая/тёмная тема с плавным переключением
- Адаптивный дизайн с закруглёнными углами
- Подсветка элементов при операциях
- Детальное логирование всех действий
- Проверка корректности дерева

## Требования
Java 17+ \
_Поддерживаемые ОС:_ Windows, macOS, Linux

## Архитектура проекта
```
src/
├── app/
│   └── Main.java                # Точка входа приложения
├── ds/
│   ├── MyList.java              # Интерфейс списка
│   ├── SimpleArrayList.java     # Реализация динамического массива
│   └── FenwickTree.java         # Основная логика дерева Фенвика
├── graph/
│   ├── Graph.java               # Интерфейс графа
│   └── AdjacencyListGraph.java  # Реализация графа на списках смежности
└── ui/
│   ├── FenwickFrame.java        # Главное окно приложения
│   └── FenwickTreePanel.java    # Панель визуализации дерева
└── module_info.java
```

Проект использует модульную систему Java:
```
module MyFenwickTree {
    requires java.desktop;
    exports app;
    exports ds;
    exports graph;
    exports ui;
}
```
